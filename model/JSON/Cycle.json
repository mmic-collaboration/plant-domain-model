{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Cycle",
    "properties": {
        "cycle id": {
            "type": "number"
        },
        "sensor event id": {
            "type": "number"
        },
        "cycle start timestamp": {
            "type": "string"
        },
        "cycle result": {
            "type": "boolean"
        },
        "cycle result code": {
            "type": "number"
        },
        "cycle end timestamp": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": [
        "cycle id",
        "sensor event id",
        "cycle start timestamp",
        "cycle result",
        "cycle result code",
        "cycle end timestamp"
    ]
}